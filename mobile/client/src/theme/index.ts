/**
 * Theme index barrel
 * Import everything theme-related from here.
 *
 *   import { useTheme, ThemeProvider, palette, spacing } from '@/theme';
 */

export * from './tokens';
export * from './theme';
export { ThemeProvider, useTheme } from './ThemeContext';
